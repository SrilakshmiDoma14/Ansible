---
- name: Setup nginx
  hosts: all
  become: yes
  tasks:
    - name: Install Nginx on Ansible-node
      apt:
        name: nginx
        state: present
        update_cache: true

      notify:
        - start nginx

  handlers:
    - name: start nginx
      service:
        name: nginx
        state: started
